// These are defined to tell a variable that it can be of any type later or at any time in future it can be changed to other type.

let id: any;
id = '32';
id = 'Jhala';
id = true;

// All of the above are valid
// Use this only one certain situations.