// Arrays
let names = ['Devraj', 'Ninja'];
names.push('Cat');
// names.push(3); Cant assign 3 to array of strings

let mixed = ['Dev', 4, true];
mixed.push('Devraj');
mixed.push(90);
mixed.push(false);
// Hence array can take any values if declared at first